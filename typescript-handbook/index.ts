const message = "Hello World!"
const user = {
    name: "Daniel",
    age: 26,
  };


function convertToLowerCase(m) {
    console.log(m.toLocaleLowerCase())
}

convertToLowerCase(message)
convertToLowerCase(user.name)
